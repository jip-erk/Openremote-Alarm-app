import type { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.openremotealarm.app",
  appName: "openremote-alarm",
  webDir: "dist",
};

export default config;
